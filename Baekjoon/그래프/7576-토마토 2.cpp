#include <vector>
#include <algorithm>
#include <iostream>
#include <cstdio>
#include <map>
#include <queue>

#define MAX 1001

#pragma warning(disable:4996)

int main()
{
	freopen("input.txt", "r", stdin);

	int n;
	int x, y;
	int board[MAX][MAX] = {0};

	scanf("%d %d", &x, &y);

	for (int i = 0; i < y; i++)
	{
		for (int j = 0; j < x; j++)
		{
			scanf("%d",)
		}
	}

	return 0;
}